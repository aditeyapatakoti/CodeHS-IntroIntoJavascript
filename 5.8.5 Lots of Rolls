function start(){
    for(var i = 0; i < 100; i++) {
        var roll = Randomizer.nextInt(1,6);
        println("You rolled a " + roll);
    }
}





//function below gets a number when rolled and then prints "You rolled a <die number>" 100 times... 
/*function start(){
	var die = Randomizer.nextInt(1,6);
	for(var i = 0; i < 100; i++){
	    println('You rolled a ' + die)
	}
}
*/
