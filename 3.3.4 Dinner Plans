function start(){
    var name = readLine("What is your name? ");
    var time = readLine('What time should we meet for dinner?');
    println('Hello ' + name + ' I will meet you at ' + time + ' o\'clock')
}
